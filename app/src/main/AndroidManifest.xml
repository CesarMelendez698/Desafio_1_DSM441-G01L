<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.miperfil">
    <!-- Declarar el permiso de la cámara -->
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MiPerfil">
        <activity
            android:name=".activity_resumen"
            android:exported="false" />
        <activity
            android:name=".activity_registro"
            android:exported="false" /> <!-- Activity 3: Perfil Guardado -->
        <activity
            android:name=".ResumenActivity"
            android:exported="false" /> <!-- Activity 2: Registro de Perfil -->
        <activity
            android:name=".RegistroActivity"
            android:exported="false" /> <!-- Activity 1: Pantalla de Bienvenida (Principal) -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>